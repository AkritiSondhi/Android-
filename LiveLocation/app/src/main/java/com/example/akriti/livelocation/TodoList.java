package com.example.akriti.livelocation;

public class TodoList {

    String[] tasks = {
            "Manage project structure",
            "Change shared preferences code to use tinyDB",
            "Make a base activity, and make menu functions a part of the base class",
            "While connecting, disable start tracking button",
    };

    String[] bugs = {
            "Cannot install app once I've uninstalled it. Need to uninstall again from apps in settings"
    };

    String[] finished = {
            "When opening app, the button says stop tracking when it's not tracking already",
            "When someone opens the app, do not default to start tracking",
            "The view switch menu shows wrong option, so one cannot switch to customer view at all",
            "Remove vibrations from the notification"
    };
}
